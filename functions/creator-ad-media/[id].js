export async function onRequestGet({env,params}){const creative=await env.TRG_ORDERS.prepare("SELECT public_object_key,content_type FROM creator_ad_creatives WHERE id=? AND validation_state='accepted'").bind(String(params.id)).first();if(!creative?.public_object_key)return new Response('Not found',{status:404});const object=await env.TRG_PRODUCTS.get(creative.public_object_key);if(!object)return new Response('Not found',{status:404});return new Response(object.body,{headers:{'cache-control':'public, max-age=3600','content-type':creative.content_type,'x-content-type-options':'nosniff'}});}
